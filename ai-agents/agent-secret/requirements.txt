fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2
# Anthropic SDK — used for the actual Claude tool-use loop.
# The agent gracefully falls back to a mock loop if ANTHROPIC_API_KEY is not set.
anthropic>=0.40,<1
# Official MCP Python SDK — client side of the Streamable HTTP transport.
mcp[cli]>=1.2.0,<2
